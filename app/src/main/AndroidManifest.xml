<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.ss">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_hdpi"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round_hdpi"
        android:supportsRtl="true"
        android:theme="@style/Theme.SS">
        <activity
            android:name=".CalculoFragment"
            android:exported="false"
            android:label="@string/title_activity_calculo_fragment"
            android:theme="@style/Theme.SS.NoActionBar" />
        <activity
            android:name=".MainActivity"
            android:theme="@style/Theme.SS.NoActionBar" />
        <activity
            android:name=".SplashScreenActivity"
            android:theme="@style/Theme.SS.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>